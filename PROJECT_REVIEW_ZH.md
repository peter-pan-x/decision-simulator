# Decision Simulator 项目检查与优化建议

## 1. 项目设计目的（我对当前实现的理解）

该项目的目标是：

- 帮助用户描述一个复杂决策（如职业/城市/投资/人生选择）。
- 通过“多 Agent 协作流水线”完成决策分析：决策解构 → 概率估计 → 时间线模拟 → 多维评估 → 风险分析 → 最终建议。
- 以可视化方式呈现结果（级联效应、雷达图、场景对比），支持中英文界面。

从当前代码看，核心分析流程由 `runCompleteAnalysis` 串联多个 Agent，且按阶段上报进度，最后再转换为 UI 使用的数据结构。整体产品形态是“前端主导 + 轻量静态托管服务”。

## 2. 当前可推导的需求（显式 + 隐式）

### 2.1 用户侧需求

- 用户可输入决策描述、2~5 个备选项、关注维度、时间跨度、风险偏好，以及自定义因素。
- 用户需要看到可理解的分析进度，并在分析完成后查看结构化结果。
- 结果需要支持多维比较和场景化表达，降低“只看单一分数”带来的误导。

### 2.2 产品/技术侧需求

- 需要兼容中英文内容展示与切换。
- 需要在无真实 API Key 的情况下仍可演示（mock 模式）。
- 需要可部署（前端构建 + Node 静态服务）。
- 需要控制前端包体积与加载时延（可视化库和 UI 组件较重）。
- 需要安全合规地处理第三方 LLM API 调用与密钥。

## 3. 发现的主要问题

> 以下问题按“影响范围 + 发生概率 + 修复收益”综合排序。

### P0（高优先级，建议尽快处理）

1. **默认几乎总是进入 Mock 模式，真实 AI 分析容易被静默禁用。**
   - `USE_MOCK_AI` 的逻辑只要任一 Key 是 mock 值就会为 `true`。
   - 但 `claude` 在当前流程里并未实际调用，默认又是 `mock-claude-key`，这会导致即使 OpenAI/Gemini 已配置，系统仍可能直接走 mock。
   - 风险：演示结果与真实能力不一致，误导验收与用户预期。

2. **在浏览器侧直接持有 OpenAI Key，存在明显泄露风险。**
   - OpenAI SDK 使用 `dangerouslyAllowBrowser: true`。
   - 风险：密钥可被前端抓包/源码恢复，无法满足生产安全要求，也不利于风控与配额控制。

3. **文档与环境变量不一致，安装即踩坑。**
   - README 要求配置 `VITE_OPENAI_API_KEY`、`VITE_GEMINI_API_KEY`。
   - 但 `.env.example` 没有这两个字段，反而是 APP 标题/Logo/Analytics 字段。
   - 风险：新同学按文档配置后仍无法跑通真实 AI 分析。

### P1（中优先级，影响体验与可维护性）

4. **国际化覆盖不完整，存在大量硬编码英文文案。**
   - 如 About 页面与结果页多个标题/说明文本直接写死英文。
   - 风险：中文用户体验割裂，与 README 中“完整双语支持”的承诺不完全一致。

5. **构建告警较多，且存在模板变量未注入问题。**
   - 构建时提示 `%VITE_APP_LOGO%`、`%VITE_APP_TITLE%` 等未定义。
   - Umami script 标签也有 bundling 兼容告警。
   - 风险：部署环境差异下可能出现运行时异常或监控脚本失效。

6. **前端主包偏大。**
   - 构建输出显示主 JS chunk 接近 1MB（minify 后仍很大）。
   - 风险：首屏加载慢，移动端体验下降，TTI 受影响。

### P2（可持续优化项）

7. **“分析仪式感延迟”固定写死，真实性能与体感不匹配。**
   - 多个阶段存在固定 `delay`，会拉长等待时间。
   - 风险：真实调用很快时显得“假慢”，真实调用很慢时又缺乏精细进度反馈。

8. **类型/依赖版本信息存在“信号不一致”。**
   - README 标注 React 19；实际运行依赖是 React 18.x。
   - 风险：团队协作时对版本能力和兼容性判断失真。

## 4. 可优化方向（按落地优先级）

### 第一阶段：先“跑对 + 跑稳”（1~3 天）

1. **修正 mock 开关策略。**
   - 仅按“当前实际用到的 provider 是否缺 key”决定是否 mock。
   - 或者引入 `VITE_USE_MOCK_AI=true/false` 显式开关，避免隐式触发。

2. **补齐 `.env.example` 与 README 对齐。**
   - 明确：本地演示最小必填项、生产部署必填项、可选监控项。

3. **为 LLM 调用增加后端代理层（至少 OpenAI）。**
   - 前端不再暴露真实 key；由后端统一注入、限流、日志与审计。

4. **清理构建告警。**
   - 给 `VITE_APP_*` 提供默认值或移除占位符。
   - 对 analytics script 做条件注入（有值才注入）。

### 第二阶段：再“体验增强 + 降本增效”（1~2 周）

5. **国际化补全与文案归口。**
   - 将 About/Results 中硬编码英文迁移到 i18n 资源。

6. **首屏与包体积优化。**
   - 对可视化组件（flow/radar）做按需懒加载。
   - 拆分页面级 chunk，减少主包。

7. **进度模型优化。**
   - 用“真实阶段耗时 + 请求状态”驱动，而非固定 sleep。
   - 对超时/失败场景给出分层错误提示（网络错误、鉴权错误、模型错误）。

### 第三阶段：产品能力深化（2~4 周）

8. **结果可信度机制。**
   - 输出“证据来源/关键假设/敏感变量”并支持用户二次调整。

9. **可复盘与可追踪。**
   - 引入决策历史版本、参数快照、结果对比。

10. **评估闭环。**
   - 建立离线评测集（典型决策场景），持续评估稳定性与可解释性。

## 5. 建议关注的关键指标（KPI）

- 首屏时间（LCP/TTI）
- 分析成功率（按 provider 分组）
- Mock 触发率（是否误触）
- API 调用失败率（鉴权/限流/解析失败）
- 用户完成率（输入 → 查看结果）
- 中英文切换覆盖率（页面文案 i18n 覆盖）

## 6. 结论

这个项目的“产品方向”和“交互骨架”是对的：它已经具备了可演示的完整链路与清晰心智模型（多 Agent + 可视化 + 双语）。当前最核心的短板不是功能数量，而是**工程可信度**（mock 逻辑、密钥安全、文档一致性、构建告警与包体积）。

如果先把 P0/P1 的工程问题收敛，再推进产品深化功能，这个项目会从“可展示 Demo”更快进入“可持续迭代的产品原型”。
